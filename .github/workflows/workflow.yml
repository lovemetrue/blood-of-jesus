# CI/CD: тесты, сборка в GHCR, деплой на VPS
name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ========== ТЕСТИРОВАНИЕ ==========
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.12']
        node: ['20']

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: 'pip'
          cache-dependency-path: 'backend/requirements.txt'

      - name: Install Python deps
        run: |
          cd backend
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-django pytest-cov

      - name: Run Django tests
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          SECRET_KEY: ${{ secrets.SECRET_KEY || 'test-secret-key-min-50-chars' }}
          DEBUG: 'True'
          ALLOWED_HOSTS: localhost,127.0.0.1
        run: |
          cd backend
          python manage.py check --deploy
          python manage.py test --noinput --parallel
          pytest --cov=. --cov-report=xml --cov-fail-under=0 || true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'

      - name: Install Node.js deps
        run: |
          cd frontend
          npm ci

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            backend/coverage.xml
            backend/.coverage

  # ========== СБОРКА И ПУШ В GHCR ==========
  build-and-push:
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (backend)
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend
          tags: |
            type=ref,event=branch
            type=sha,prefix=,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Extract metadata (frontend)
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix=,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ========== ДЕПЛОЙ НА VPS ==========
  deploy:
    needs: build-and-push
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    env:
      DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '/www/blood-of-jesus' }}
      IMAGE_TAG: ${{ github.ref_name }}
      GHCR_IMAGE: ${{ github.repository }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}
          ssh-passphrase: ${{ secrets.VPS_SSH_PASSPHRASE }}

      - name: Deploy to VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}
        run: |
          BRANCH="${{ github.ref_name }}"
          DEPLOY_PATH="${{ vars.DEPLOY_PATH || '/www/blood-of-jesus' }}"
          # Скрипт: путь и ветка подставляются здесь; токен/образ — через env по SSH
          cat > deploy.sh << DEPLOY_EOF
          #!/bin/bash
          set -e
          echo "=== Деплой ==="
          cd "$DEPLOY_PATH"

          git fetch origin
          git reset --hard origin/$BRANCH

          if [ ! -f .env ]; then
            cp .env.example .env
            echo "WARNING: Отредактируйте .env (POSTGRES_PASSWORD, SECRET_KEY)"
          fi

          echo "\$GHCR_TOKEN" | docker login ghcr.io -u "\$GITHUB_ACTOR" --password-stdin

          export GHCR_IMAGE="\$GHCR_IMAGE"
          export IMAGE_TAG="\$IMAGE_TAG"
          docker compose -f docker-compose.prod.yml pull
          docker compose -f docker-compose.prod.yml down
          docker compose -f docker-compose.prod.yml up -d

          docker compose -f docker-compose.prod.yml exec -T backend python manage.py migrate --noinput
          docker compose -f docker-compose.prod.yml exec -T backend python manage.py collectstatic --noinput --clear

          docker system prune -f
          echo "=== Готово ==="
          DEPLOY_EOF

          chmod +x deploy.sh
          scp -o StrictHostKeyChecking=no deploy.sh "$VPS_USER@$VPS_HOST:$DEPLOY_PATH/"
          ssh -o StrictHostKeyChecking=no "$VPS_USER@$VPS_HOST" \
            "export GHCR_IMAGE='$GHCR_IMAGE'; export IMAGE_TAG='$IMAGE_TAG'; export GITHUB_ACTOR='$GITHUB_ACTOR'; export GHCR_TOKEN='$GHCR_TOKEN'; cd $DEPLOY_PATH && bash deploy.sh"

      - name: Health check
        run: |
          sleep 15
          curl -sf "https://${{ vars.DOMAIN }}/api/health/" || curl -sf "http://${{ secrets.VPS_HOST }}:80/api/health/" || true

  # ========== РЕЛИЗ (при пуше тега v*) ==========
  release:
    needs: build-and-push
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          body: |
            ## Docker Images (GHCR)
            - Backend: `${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend:${{ github.ref_name }}`
            - Frontend: `${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-frontend:${{ github.ref_name }}`
            - Domain: https://${{ vars.DOMAIN }}
          draft: false
          generate_release_notes: true
